# ContentCapture Pro - Quick Reference Card

## 🎯 Essential Shortcuts

| Keys | What it does |
|------|--------------|
| `Ctrl+Alt+P` | Capture webpage |
| `Ctrl+Alt+Space` | Quick search |
| `Ctrl+Alt+B` | Browse captures |
| `Ctrl+Alt+F12` | Help |

## 📝 Hotstring Pattern

For a capture named `mylink`:

- `::mylink::` → Paste content
- `::mylink?::` → Action menu
- `::mylinkem::` → Email it
- `::mylinkgo::` → Open URL
- `::mylinkfb::` → Facebook
- `::mylinkx::` → Twitter/X

## 🔥 Pro Tips

1. **Quick capture**: Just hit `Ctrl+Alt+P`, give it a short name, done!
2. **Find anything**: `Ctrl+Alt+Space` searches all your captures instantly
3. **Share smart**: Social suffixes auto-check character limits
4. **Stay organized**: Use tags when capturing for easy filtering later

## 📍 Where's my data?

- `captures.dat` - Your captures (plain text, portable)
- `config.ini` - Your settings
- `ContentCapture_Generated.ahk` - Auto-generated hotstrings

## ⚡ First Thing to Do

Press `Ctrl+Alt+P` in your browser and capture something!
